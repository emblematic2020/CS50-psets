// Define max no of voters at 100, and max no of candidates 9
// preferences[i][j] is the index of candidate who is  jth pref for voter i, ie rank of candidate for voter i. e.g. preference[0][1] means voter ranked the first candidate second
// Candidates have name, vote count, eliminated status
// Array of candidates
// Declare variable integeres for numbers of voters and candidates
// Function prototypes
bool vote(int voter, int rank, string name);
void tabulate(void);
bool print_winner(void);
int find_min(void);
bool is_tie(int min);
void eliminate(int min);

//main function -->
    // Check for invalid usage
    // check for too many candidates (ie more than 9)
    // Populate array of candidates (initialising their votes to 0, and elimination status at FALSE for now). remember i is count of voters.
    // Check if invalid number of voters
    // Keep querying for votes
        // Query for each rank
        // Record vote, unless it's invalid. ie if
    // Keep holding runoffs until winner exists
        // Calculate votes given remaining candidates - look at all voters’ prefs and compute current vote totals, by looking at each voter’s top choice candidate who hasn’t yet been eliminated.
        // Check if election has been won
       // Eliminate last-place candidates
        // If tie, everyone wins
        // Otherwise, Eliminate anyone with minimum number of votes
        // Reset vote counts back to zero

functions
// // Record preference if vote is valid
bool vote(int voter, int rank, string name)

// Tabulate votes for non-eliminated candidates - with void tabulate(void)

// Print the winner of the election, if there is one- with bool print_winner(void)

// Return the minimum number of votes any remaining candidate has - int find_min(void)

// Return true if the election is tied between all candidates, false otherwise - with bool is_tie(int min)

// Eliminate the candidate (or candidiates) in last place - with void eliminate(int min)

